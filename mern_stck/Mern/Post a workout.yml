info:
  name: Post a workout
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/api/workout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
