info:
  name: Post a workout
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/api/workout/
  body:
    type: json
    data: |-
      {
        "title":"Situps",
        "reps":0,
        "load":30
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
