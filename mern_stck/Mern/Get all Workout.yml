info:
  name: Get all Workout
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/api/workout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
